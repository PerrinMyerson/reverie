// Negative runtime example: the conditional exit assertion lies.
//
// The true branch requires the trailing `fi` assertion to be true afterward,
// but this program always exits with `false`:
//   reverie run examples/if_assertion_failure.rev --var x=0 --var y=0
if x == 0 then
  x += 1
else
  y += 1
fi false
