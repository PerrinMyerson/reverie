// Janus-style optional control-flow clauses.
//
// Omitted `else`, `do`, and `loop` clauses are parsed as `skip`.

local x: int = 0
  if x == 0 then
    x += 1
  fi x == 1;

  from x == 1 loop
    x += 1
  until x == 2;

  from x == 2 do
    x -= 2
  until x == 0
delocal x = 0
